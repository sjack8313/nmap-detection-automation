# This script simulates SOAR-style remediation for network scans
# It would be triggered when a detection fires and automatically block the attacker IP

def block_ip(ip_address):
    print(f"[SOAR] Blocking IP: {ip_address}")
    # Simulated API request to firewall or SOAR platform
    # In Splunk SOAR, this would be a playbook step using REST or custom connector

# Example IP flagged by detection logic
block_ip("192.168.1.100")
